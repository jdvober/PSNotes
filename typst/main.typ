// Recommended Packages from typst.app

// A package to render skeletal formulas using CeTZ
// #import "@preview/alchemist:0.1.8"

//  Drawing Diagrams - Drawing with Typst made easy, providing an API inspired by TikZ and Processing. Includes modules for plotting, charts and tree layout.
#import "@preview/cetz:0.4.2"

//  Decks of cards
#import "@preview/deckz:0.3.1"

//  Slides / Presentations
#import "@preview/touying:0.6.1"

//  SI Units
#import "@preview/unify:0.7.1"

//  Glossary
#import "@preview/glossarium:0.5.9": make-glossary, register-glossary, print-glossary, gls, glspl

//  A Typst package for typesetting proof trees.
// #import "@preview/curryst:0.5.1": rule, prooftree

//  Helpful functions for content positioning and margin comments/notes
// #import "@preview/drafting:0.2.2"

//  Various enhancements for mathematical expressions.
#import "@preview/equate:0.3.2"

//  Typst-setting finite automata with CeTZ
// #import "@preview/finite:0.5.0"

//  Draw diagrams with nodes and arrows.
#import "@preview/fletcher:0.5.8"

// Beautiful, flexible, and integrated. Display custom frames for theorems, environments, and more. Attractive visuals with syntax that blends seamlessly into the source.
#import "@preview/frame-it:1.2.0"

// A package for typesetting games in Typst.
// #import "@preview/game-theoryst:0.1.0"

// Query and display headings in your documents and templates.
#import "@preview/hydra:0.6.2"

// Scientific data visualization.
#import "@preview/lilaq:0.5.0"

// Algorithms in pseudocode, unopinionated and flexible
// #import "@preview/lovelace:0.3.0"

// Style matrices with custom paths, strokes and fills for appealing visualizations.
// #import "@preview/pavemat:0.2.0"

// Math constructs for science and engineering: derivative, differential, vector field, matrix, tensor, Dirac braket, hbar, transpose, conjugate, many operators, and more.
// #import "@preview/physica:0.9.6"

// Relative positioning by pins, especially useful for making slides in typst.
// #import "@preview/pinit:0.2.2"

// Presentation slides creation with Typst
// #import "@preview/polylux:0.4.0"

// Custom shorthands for math equations.
// #import "@preview/quick-maths:0.2.1"

// Colorful and customizable boxes for Typst
// #import "@preview/showybox:2.0.4"

// A highly efficient random number generator for Typst
// #import "@preview/suiji:0.4.0"

// Barcode and QRCode generator for Typst using Zint.
// #import "@preview/tiaoma:0.3.0"

// Documentation generator for Typst code in Typst.
// #import "@preview/tidy:0.4.3"

// Arrows and other marks for lines and paths.
// #import "@preview/tiptoe:0.3.1"

// A powerful package for creating presentation slides in Typst.
// #import "@preview/touying:0.6.1"

// Basic shadows for Typst
// #import "@preview/umbra:0.1.1"

// Word counts and document statistics.
// #import "@preview/wordometer:0.1.5"

// Wrap text around figures and content
// #import "@preview/wrap-it:0.1.1"

// A lightweight and fast package for displaying code blocks with line numbers or highlighting.
// #import "@preview/zebraw:0.5.5"

// Advanced scientific number formatting.
// #import "@preview/zero:0.5.0"


// Other Packages

// An Abbreviations package.
// #import "@preview/abbr:0.2.3"

// Manage acronyms and their definitions in Typst.
// #import "@preview/acrotastic:0.1.1"

// Simple page numbering of front and back matter.
// #import "@preview/anti-matter:0.1.1"

// !!!!!!Draw Atoms, their electron configurations, shells and orbitals in Typst.
// #import "@preview/atomic:1.0.0"

// A package to create callouts.
// #import "@preview/babble-bubbles:0.1.0"

// Adds styled badges, boxes and menu actions.
// #import "@preview/badgery:0.1.1"

// Generate and get backlinks.
// #import "@preview/basalt-backlinks:0.1.1"

// !!!!!Package to insert clear TODOs, optionally with an outline.
// #import "@preview/big-todo:0.2.0"

// Display chessboards.
// #import "@preview/board-n-pieces:0.6.0"

// Table styling, inspired by LaTeX's Booktabs package.
// #import "@preview/booktabs:0.0.4"

// Generate QR codes in typst.
// #import "@preview/cades:0.3.0"

// Dead simple, printable, flashcards with Typst.
// #import "@preview/cartao:0.2.0"

// Plotting module for CeTZ.
// #import "@preview/cetz-plot:0.1.3"

// CeTZ library for drawing venn diagrams for two or three sets.
// #import "@preview/cetz-venn:0.1.4"

// Write markdown-like checklist easily.
// #import "@preview/cheq:0.3.0"

// Typst package for creating elegant headers and footers
// #import "@preview/chic-hdr:0.5.0"

// A package to create calendar with events.
// #import "@preview/cineca:0.5.0"

// !!!!!Makes a Bibtex bibliography available as a Typst dictionary.
// #import "@preview/citegeist:0.2.0"

// !!!!!Typst package for curving text on an arc or circle
// #import "@preview/curvly:0.1.0"

// Draw graphs with Graphviz. Use mathematical formulas as labels.
// #import "@preview/diagraph:0.3.6"

// Smoothly loads environment variables from dotenv (.env) files
// #import "@preview/dovenv:0.1.0"

// A comprehensive Typst template for creating Dungeons & Dragons 5E content including stat blocks, spell cards, and adventure modules.
// #import "@preview/dragonling:0.2.0"

// !!!!!A suite of customizable block environments for taking notes and doing problem sets, especially in Mathematics, Computer Science, and Physics.
// #import "@preview/ergo:0.1.1"

// A Typst package to organise exercises and defer their solutions.
// #import "@preview/exercism:1.0.0"

// !!!!!Draw atomic or ionic structures powered by CeTZ and single or double line bridge.
// #import "@preview/ezchem:0.1.0"

// Format numbers and units with style.
// #import "@preview/fancy-units:0.1.1"

// A Typst library for Font Awesome icons through the desktop fonts.
// #import "@preview/fontawesome:0.6.0"

// Operations on fractions.
// #import "@preview/fractus:0.1.0"

// Replace letters in equations with fruit emoji
// #import "@preview/fruitify:0.1.1"

// Package providing convenient functional functions to use on arrays.
// #import "@preview/funarray:0.4.0"

// Simple component to show a value between 0 and 1 on a nice gradient slider
// #import "@preview/gradslide:0.1.0"

// Generate graphs using the graphviz dot language.
// #import "@preview/gviz:0.1.0"

// Make counters inherit from the heading counter.
// #import "@preview/headcount:0.1.0"

// !!!!!A package for making linear timelines, inspired by chronology.
// #import "@preview/herodot:0.3.0"

// !!!!!Configurable figure and equation numbering per section.
// #import "@preview/i-figured:0.2.4"

// !!!!!A package to colors matching parenthesis
// #import "@preview/iridis:0.1.0"

// An easy way to customize enum and list.
// #import "@preview/itemize:0.1.2"

// !!!!!!!!!!QuickJS JavaScript runtime for Typst
// #import "@preview/jogs:0.2.4"

// !!!!!This package provides a simple way to style keyboard shortcuts in your documentation.
// #import "@preview/keyle:0.2.0"

// Add layers, toggle them using tags easily
// #import "@preview/lasagna:0.1.0"

// Build elegent slide decks with Typst
// #import "@preview/lineal:0.1.0"

// Magnify part of image or content.
// #import "@preview/magnifying-glass:0.1.0"

// !!!!!!!!!!!!!!!A package for marking and annotating in math blocks.
// #import "@preview/mannot:0.3.0"

// !!!!!!!!!!!!!!!Easy-to-use but powerful and smart margin notes.
// #import "@preview/marge:0.1.0"

// Configurable margin-notes and matching wide blocks.Configurable margin-notes and matching wide blocks.
// #import "@preview/marginalia:0.2.3"

// !!!!!Typeset multi-page chat histories, including from external JSON files.
// #import "@preview/messeji:0.3.0"

// Gnuplot in Typst
// #import "@preview/neoplot:0.0.3"

// Nordic color theme.
// #import "@preview/nordic:0.1.0"

// Add english ordinals to numbers, eg. 1st, 2nd, 3rd, 4th.
// #import "@preview/nth:1.0.1"

// A package that helps you to specify different numbering formats for different levels of headings.
// #import "@preview/numbly:0.1.0"

// !!!!!Automatically adjust the text size to make it fit on one line filling the available space.
// #import "@preview/one-liner:0.2.0"

// !!!!!Forge wonderful chat messages
// #import "@preview/ourchat:0.2.0"

// Easier customization of outline entries.
// #import "@preview/outrageous:0.4.0"

// Create fancy-looking homebrewn Dungeons and Dragons 2024 edition
// #import "@preview/owlbear:0.0.1"

// !!!!!Convenient Rust-like string formatting in Typst
// #import "@preview/oxifmt:1.0.0"

// 3D plotting for surfaces and parametric equations using CeTZ similar to pgfplots for LaTeX
// #import "@preview/plotsy-3d:0.2.1"

// A package to create slides with ease.
// #import "@preview/presentate:0.2.0"

// A mathematical brackets colorizer.
// #import "@preview/prismath:0.1.0"

// Run python code in typst.
// #import "@preview/pyrunner:0.3.0"

// Easy flashcards with customizable look and feel
// #import "@preview/quick-cards:0.1.1"

// !!!!!!!!!!!!!!!Parsing xlsx file into a typst table, powered by wasm.
// #import "@preview/rexllent:0.3.4"

// Counters which can inherit from other counters.
// #import "@preview/rich-counters:0.2.2"

// A library for building exams, tests, etc. with Typst
// #import "@preview/scrutinize:0.3.0"

// !!!!Typst library for rounding numbers with significant figures and measurement uncertainty.
// #import "@preview/sigfig:0.1.0"

// !!!!!!!!!!Smart handling of consecutive references (i.e. cleveref for Typst).
// #import "@preview/smartaref:0.1.0"

// !!!!!!!!!!!!!!!!!!!!A package to formulate the solution to a physical problem
// #import "@preview/solving-physics:0.1.0"

// !!!!!!!!!!!!!!!!!!!!A carefully curated collection of font pairings.
// #import "@preview/tasteful-pairings:0.1.0"

// Make simple tierlists using the Catppuccin pastel color palettes.
// #import "@preview/tierpist:0.1.0"

// Simple, consistent, and appealing math homework template
// #import "@preview/tinyset:0.1.0"

// !!!!!A collection of tools to make a teachers life easier.
// #import "@preview/ttt-utils:0.1.4"

// !!!!!!!!!!!!!!!!!!!Creates trees from lists using Cetz.
// #import "@preview/twig:0.1.0"

// !!!!!!!!!!!!!!!!!!!!Typeset chemical formulas and reactions.
// #import "@preview/typsium:0.3.0"

// !!!!!!!!!!!!!!!!!!!Draw Atoms, their electron configurations, shells and orbitals in Typst.
// #import "@preview/typsium-atomic:0.1.0"

// !!!!!!!!!!Display and format Hazard & Precautionary statements and GHS pictograms in Typst.
// #import "@preview/typsium-ghs:0.1.0"

// Display Warning signs, Fire signs, Emergency signs, Mandatory signs, Prohibited signs following the ISO 7010 standard in Typst
// #import "@preview/typsium-iso-7010:0.1.0"

// Classes/structs, pattern matching, safe counters... and more! Your one-stop library for programming tools not already in core Typst.
// #import "@preview/typsy:0.1.0"

// !!!!!!!!!!!!!!!Variable-length arrows in Typst.
// #import "@preview/xarrow:0.3.1"

// !!!!!!!!!!!!!!!!!!A package to draw amazing electronic circuits using CeTZ superpowers
// #import "@preview/zap:0.4.0"
